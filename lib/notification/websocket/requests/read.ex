import Helix.Websocket.Request

request Helix.Notification.Websocket.Requests.Read do

  def check_params(request, socket) do
  end

  def check_permissions(request, socket) do
  end

  def handle_request(request, socket) do
  end

  render_empty()

end
