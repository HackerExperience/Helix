defmodule Helix.Server.Internal.MotherboardTest do

  use Helix.Test.Case.Integration

  describe "setup/2" do
    test "inserts the mobo and all its components" do
      # Before this, I need ComponentInternal ...
    end
  end
end
